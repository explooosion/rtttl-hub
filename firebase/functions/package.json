{
  "name": "rtttl-hub-functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "deploy": "npx firebase --config ../firebase.json --project rtttl-hub deploy --only functions",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@polar-sh/sdk": "^0.34.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20.0.0",
    "typescript": "~6.0.3"
  }
}
